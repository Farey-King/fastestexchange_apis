# Exchange Rate API Configuration
# Add these to your .env file to enable dynamic exchange rates

# Fixer.io API (https://fixer.io/)
# Free tier: 100 requests/month
FIXER_API_KEY=your_fixer_api_key_here

# ExchangeRate-API (https://exchangerate-api.com/)
# Free tier: 1,500 requests/month
EXCHANGERATE_API_KEY=your_exchangerate_api_key_here

# CurrencyAPI (https://currencyapi.com/)
# Free tier: 300 requests/month
CURRENCY_API_KEY=your_currency_api_key_here

# Redis configuration for caching (optional, will use default Django cache if not configured)
REDIS_URL=redis://localhost:6379/1

# Django Cache Configuration (add to settings.py)
# CACHES = {
#     'default': {
#         'BACKEND': 'django_redis.cache.RedisCache',
#         'LOCATION': 'redis://127.0.0.1:6379/1',
#         'OPTIONS': {
#             'CLIENT_CLASS': 'django_redis.client.DefaultClient',
#         }
#     }
# }

# Logging Configuration for Exchange Rate Service (add to settings.py)
# LOGGING = {
#     'version': 1,
#     'disable_existing_loggers': False,
#     'formatters': {
#         'verbose': {
#             'format': '{levelname} {asctime} {module} {process:d} {thread:d} {message}',
#             'style': '{',
#         },
#     },
#     'handlers': {
#         'exchange_rates_file': {
#             'level': 'INFO',
#             'class': 'logging.FileHandler',
#             'filename': 'logs/exchange_rates.log',
#             'formatter': 'verbose',
#         },
#     },
#     'loggers': {
#         'fastest_exchange.exchange_rate_service': {
#             'handlers': ['exchange_rates_file'],
#             'level': 'INFO',
#             'propagate': True,
#         },
#     },
# }

# Cron job setup for automatic rate updates
# Add this to your crontab (crontab -e):
# # Update exchange rates every hour
# 0 * * * * /path/to/venv/bin/python /path/to/project/manage.py update_exchange_rates >> /var/log/exchange_rates_cron.log 2>&1
# 
# # Update exchange rates every 30 minutes (more frequent)
# */30 * * * * /path/to/venv/bin/python /path/to/project/manage.py update_exchange_rates >> /var/log/exchange_rates_cron.log 2>&1
